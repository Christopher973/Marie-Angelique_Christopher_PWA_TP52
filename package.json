{
  "name": "temperature-converter-pwa",
  "version": "1.0.0",
  "description": "Application de conversion de températures en PWA",
  "scripts": {
    "build-sw": "workbox injectManifest workbox-config.js",
    "start": "http-server -p 8080 -c-1",
    "build": "npm run build-sw && npm start"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "workbox-cli": "^7.0.0"
  }
}
